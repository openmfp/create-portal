name: pipe
on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize

permissions:
  # Write access is used for tag creation and version commits
  contents: write
  # Needed for provenance creation when publishing to npmjs
  id-token: write

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  pipe:
    uses: openmfp/gha/.github/workflows/pipeline-node-module.yml@a9dbe8820bfb835044ed5b41194ab431c8ff7967 # main
    secrets: inherit
    with:
      publish: true
      hasBuild: false
      environment: npmjs:@openmfp/create-portal