name: pipe
on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize

permissions:
  # Write access is used for tag creation and version commits
  contents: write
  # Needed for provenance creation when publishing to npmjs
  id-token: write

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  pipe:
    uses: openmfp/gha/.github/workflows/pipeline-node-module.yml@3901a2f18cf3795bc3da7272a188624d7d6252ae # main
    secrets: inherit
    with:
      publish: true
      hasBuild: false
      environment: npmjs:@openmfp/create-portal